NAME:K.Sandhya
Roll no:24881A1232
BRANCH/SECTION: Information technology - A
COURCE:Web technology 
WEEK1:This HTML code creates a Mobile Store webpage with a pink background. It displays the student's name and roll number, a table of mobile products, their images, specifications, feature links, and YouTube video links for Samsung Galaxy and iPhone 14.
WEEK2:This HTML program uses frames to create a college website layout. The top frame displays the college name (marquee), logo, student name, and roll number. The left frame contains navigation links (About College, Address, Info), and the right frame displays the corresponding college information when a link is clicked. It demonstrates the use of <frameset>, <frame>, <a>, <marquee>, <img>, and <p> tags.
WEEK3:This HTML code creates a Registration Form with a light green background. It collects user details such as name, gender, languages known, email, mobile number, address, job location, and resume upload. It also displays the student's name and roll number and includes Submit and Reset buttons. The form demonstrates the use of HTML form elements like <form>, <input>, <textarea>, <select>, and <table>.
WEEK4:This HTML program creates an SBI Bank EMI Calculator using HTML, CSS, and JavaScript. It accepts the user's name, occupation, loan amount, interest rate, and loan tenure. When the Calculate EMI button is clicked, JavaScript calculates the monthly EMI using the standard formula and displays the result on the webpage. It demonstrates the use of forms, CSS styling, JavaScript functions, and DOM manipulation.
WEEK5:This HTML program creates an Email Registration Form using HTML and JavaScript. It collects the user's username, email, and mobile number and validates them using regular expressions (Regex). If all inputs are valid, it displays a "Registration Successfully" message; otherwise, it highlights the invalid field and shows an "Invalid" alert. It demonstrates form validation using JavaScript.
